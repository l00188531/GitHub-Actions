module.exports = {
    testMatch: ['<rootDir>/tests/**/*.js'], // Match all .js files in the 'tests' folder
    // Alternative: Use a custom regex if needed
    // testRegex: 'tests/.*\\.js$',
  };
  


