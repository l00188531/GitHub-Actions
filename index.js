// index.js

// Adds two numbers
function add(a, b) {
    return a + b;
  }
  
  // Subtracts the second number from the first
  function subtract(a, b) {
    return a - b;
  }
  
  module.exports = { add, subtract };
  